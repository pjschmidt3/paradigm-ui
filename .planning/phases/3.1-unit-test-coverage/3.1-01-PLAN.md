---
phase: 3.1-unit-test-coverage
plan: 01
type: execute
---

<objective>
Add unit tests for typography components (Heading, Paragraph).

Purpose: Establish test coverage for foundational typography components used throughout the registry.
Output: Test files `heading.test.tsx` and `paragraph.test.tsx` with comprehensive coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@registry/new-york/ui/__tests__/button.test.tsx (pattern reference)
@registry/new-york/ui/heading.tsx
@registry/new-york/ui/paragraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Heading component tests</name>
  <files>registry/new-york/ui/__tests__/heading.test.tsx</files>
  <action>
Create comprehensive tests for Heading component following button.test.tsx patterns:

**Test Groups:**
1. Basic rendering - renders with children, applies base classes
2. Heading levels - each level (1-5) renders correct element (h1-h5) with appropriate classes
3. Custom className - merges with default classes via cn()
4. Accessibility - heading elements are accessible by role

**Key assertions:**
- Level 1: h1 element with text-4xl, font-bold
- Level 2: h2 element with text-2xl, font-medium
- Level 3: h3 element with text-xl, font-medium
- Level 4: h4 element with text-lg, font-medium
- Level 5: h5 element with text-md, font-medium
- All levels: scroll-m-20, text-primary, font-roboto

Use screen.getByRole('heading', { level: X }) for accessibility queries.
  </action>
  <verify>npm test -- heading.test.tsx --passWithNoTests=false passes</verify>
  <done>All 5 heading levels tested, className merging verified, accessibility queries work</done>
</task>

<task type="auto">
  <name>Task 2: Create Paragraph component tests</name>
  <files>registry/new-york/ui/__tests__/paragraph.test.tsx</files>
  <action>
Create tests for Paragraph component:

**Test Groups:**
1. Basic rendering - renders p element with children
2. Default classes - applies font-mono, text-lg, font-light
3. Custom className - merges with defaults
4. HTML attributes - spreads additional props (data-testid, etc.)

**Key assertions:**
- Renders as p element
- Has default typography classes
- className prop merges correctly
- Supports standard HTML paragraph attributes
  </action>
  <verify>npm test -- paragraph.test.tsx --passWithNoTests=false passes</verify>
  <done>Paragraph rendering, styling, and prop spreading all tested</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- heading.test.tsx` passes all tests
- [ ] `npm test -- paragraph.test.tsx` passes all tests
- [ ] No TypeScript errors in test files
- [ ] Tests follow established patterns from button.test.tsx
</verification>

<success_criteria>

- Both test files created and passing
- All heading levels (1-5) covered
- Paragraph basic functionality covered
- Tests use Testing Library best practices (role queries, accessible assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-unit-test-coverage/3.1-01-SUMMARY.md`
</output>
