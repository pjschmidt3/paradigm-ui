---
phase: 11-marketing-site-sidebar
plan: 01
type: execute
---

<objective>
Create the documentation shell with sidebar layout and navigation structure.

Purpose: Establish the `/docs` route with shadcn-style sidebar navigation using existing Sidebar component.
Output: Working docs layout with collapsible sidebar, navigation config, and route structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@site/src/app/layout.tsx
@site/src/components/nav.tsx
@site/src/components/paradigm.ts
@src/components/ui/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs layout with sidebar</name>
  <files>site/src/app/docs/layout.tsx</files>
  <action>
    Create a docs layout using SidebarProvider, Sidebar, and SidebarInset from the existing sidebar component. The layout should:
    - Import sidebar components from paradigm.ts (add exports first)
    - Use SidebarProvider wrapping the entire layout
    - Sidebar on left with collapsible behavior (collapsible="icon" on mobile)
    - SidebarInset for main content area
    - Include SidebarHeader with "Documentation" title and version badge
    - Include SidebarContent with navigation groups
    - Responsive: sidebar sheet on mobile, fixed on desktop
    - Match the site's existing color scheme (zinc-based)

    Also update site/src/components/paradigm.ts to export sidebar components:
    - Sidebar, SidebarProvider, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarGroupContent
    - SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarHeader, SidebarInset, SidebarTrigger
  </action>
  <verify>npm run dev in site/, visit /docs - should see sidebar layout skeleton</verify>
  <done>Docs layout renders with sidebar shell, no 404, no console errors</done>
</task>

<task type="auto">
  <name>Task 2: Create navigation config and DocsSidebar component</name>
  <files>site/src/components/docs-sidebar.tsx, site/src/lib/docs-nav.ts</files>
  <action>
    Create docs-nav.ts with navigation structure:
    ```
    type NavItem = { title: string; href: string; items?: NavItem[] }
    type NavSection = { title: string; items: NavItem[] }

    export const docsNav: NavSection[] = [
      {
        title: "Getting Started",
        items: [
          { title: "Introduction", href: "/docs" },
          { title: "Installation", href: "/docs/installation" }
        ]
      },
      {
        title: "Components",
        items: [
          { title: "Layout", href: "/docs/components/layout" },
          { title: "Typography", href: "/docs/components/typography" },
          { title: "Cards", href: "/docs/components/cards" }
        ]
      },
      {
        title: "Hooks",
        items: [
          { title: "useDisclosure", href: "/docs/hooks/use-disclosure" },
          { title: "useToggle", href: "/docs/hooks/use-toggle" },
          { title: "useMediaQuery", href: "/docs/hooks/use-media-query" },
          { title: "useClickOutside", href: "/docs/hooks/use-click-outside" }
        ]
      },
      {
        title: "Blocks",
        items: [
          { title: "Overview", href: "/docs/blocks" },
          { title: "Marketing", href: "/docs/blocks/marketing" }
        ]
      }
    ]
    ```

    Create DocsSidebar component that:
    - Imports docsNav config
    - Uses usePathname() to highlight active item
    - Renders SidebarGroup for each section with SidebarGroupLabel
    - Renders SidebarMenu/SidebarMenuItem/SidebarMenuButton for items
    - Uses Next.js Link component with asChild on SidebarMenuButton
    - Style active items with isActive prop based on current path

    Update docs/layout.tsx to use DocsSidebar in SidebarContent.
  </action>
  <verify>npm run dev, visit /docs - sidebar shows all navigation sections, clicking items navigates (404 expected for unbuilt pages)</verify>
  <done>Sidebar navigation renders with all sections, active state highlights current page, links navigate correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` in site/ succeeds
- [ ] /docs route renders sidebar layout
- [ ] Navigation sections display (Getting Started, Components, Hooks, Blocks)
- [ ] Sidebar collapses on mobile (test with responsive view)
- [ ] Active page highlighting works
</verification>

<success_criteria>

- Both tasks completed
- All verification checks pass
- Docs shell ready for content pages
</success_criteria>

<output>
After completion, create `.planning/phases/11-marketing-site-sidebar/11-01-SUMMARY.md`
</output>
