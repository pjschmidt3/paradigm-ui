---
phase: 02-component-tiering
plan: 03
type: execute
---

<objective>
Register second batch of atomic components from src/components/ui as free tier items (E-P alphabetically).

Purpose: Continue adding basic shadcn components to registry with free tier metadata.
Output: Registry.json expanded with additional atomic component items (empty through progress).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-tiering/02-02-SUMMARY.md (if exists)
@registry.json
@src/components/ui/

**Components to register (batch 2 - E through P):**
1. empty
2. field
3. form
4. hover-card
5. input-group
6. input-otp
7. input
8. item
9. kbd
10. label
11. menubar
12. navigation-menu
13. pagination
14. popover
15. progress

**Note:** src/components/ui/button.tsx exists but registry already has premium button from registry/new-york/ui/button.tsx. Register as "shadcn-button" to differentiate, or skip if they're the same component.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register form input components (E-I)</name>
  <files>registry.json</files>
  <action>
Add registry items for these components:

1. empty - feedback category, minimal deps
2. field - form category, likely depends on form utilities
3. form - form category, depends on react-hook-form, @hookform/resolvers
4. hover-card - overlay category, depends on @radix-ui/react-hover-card
5. input-group - form category, minimal deps
6. input-otp - form category, depends on input-otp
7. input - form category, minimal deps
8. item - misc category, minimal deps
9. kbd - misc category, minimal deps

For each:
1. Read file to identify dependencies
2. Add to registry with meta.tier="free", appropriate categories
3. Include registryDependencies: ["utils"] where cn() is used
  </action>
  <verify>Count new items added: 9</verify>
  <done>9 form/input components registered as free tier</done>
</task>

<task type="auto">
  <name>Task 2: Register navigation and overlay components (L-P)</name>
  <files>registry.json</files>
  <action>
Add registry items for remaining E-P components:

1. label - form category, depends on @radix-ui/react-label
2. menubar - navigation category, depends on @radix-ui/react-menubar
3. navigation-menu - navigation category, depends on @radix-ui/react-navigation-menu
4. pagination - navigation category, minimal deps
5. popover - overlay category, depends on @radix-ui/react-popover
6. progress - feedback category, depends on @radix-ui/react-progress

For each:
1. Read file to identify exact dependencies
2. Add to registry with meta.tier="free"
3. Assign appropriate categories
  </action>
  <verify>Count total items should be 48 (33 from batch 1 + 15 from batch 2)</verify>
  <done>6 more components registered, total 48 items</done>
</task>

<task type="auto">
  <name>Task 3: Validate batch 2</name>
  <files>registry.json</files>
  <action>
Validate all batch 2 items:
1. Verify JSON valid: `cat registry.json | jq '.'`
2. Check all new items have tier metadata
3. Verify no duplicate names
4. Check all file paths exist
5. Fix any issues
  </action>
  <verify>JSON valid, 48 total items, all have tier metadata</verify>
  <done>Batch 2 complete with 15 additional atomic components as free tier</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Valid JSON
- [ ] Total items: 48 (33 + 15)
- [ ] All new items have meta.tier: "free"
- [ ] No duplicate names
- [ ] All file paths exist
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 15 new atomic components registered
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-tiering/02-03-SUMMARY.md`
</output>
