---
phase: 02-component-tiering
plan: 01
type: execute
---

<objective>
Add tier metadata and categories to existing registry items to distinguish free vs premium components.

Purpose: Enable freemium distribution by marking components with tier information the CLI can filter on.
Output: Updated registry.json with all 14 existing items having tier metadata and category organization.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-registry-foundation/01-01-SUMMARY.md
@registry.json

**Tier Classification Rules (from PROJECT.md):**
- Free tier: Basic atomic components, utilities, simple building blocks
- Premium tier: Advanced composite components that combine multiple components, animations, complex features

**Current Registry Items (14):**
- Lib items (3): utils, box-types, helper-types → Free (utilities)
- UI items (11): box, flex, flex-row, flex-col, grid, heading, paragraph, button, social-links, hero, appear

**Tier Assessment:**
- Free: utils, box-types, helper-types, heading, paragraph (basic utilities and simple typography)
- Premium: box, flex, flex-row, flex-col, grid, button, social-links, hero, appear (composite/layout/animated)

**Category Organization:**
- layout: box, flex, flex-row, flex-col, grid
- typography: heading, paragraph
- animation: appear, social-links
- composite: hero
- form: button
- utilities: utils, box-types, helper-types
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier metadata to lib items</name>
  <files>registry.json</files>
  <action>
Update the 3 lib items (utils, box-types, helper-types) to include:
- `"meta": { "tier": "free" }` - utilities should always be free
- `"categories": ["utilities"]` - organize in utilities category

Structure for each lib item:
```json
{
  "name": "utils",
  "type": "registry:lib",
  "meta": { "tier": "free" },
  "categories": ["utilities"],
  ...existing fields
}
```
  </action>
  <verify>Run `cat registry.json | jq '.items[] | select(.type == "registry:lib") | {name, meta, categories}'` to confirm all 3 lib items have tier and categories</verify>
  <done>All 3 lib items have meta.tier="free" and categories=["utilities"]</done>
</task>

<task type="auto">
  <name>Task 2: Add tier metadata to UI items</name>
  <files>registry.json</files>
  <action>
Update the 11 UI items with tier and categories:

**Free tier (2 items) - simple typography:**
- heading: `meta: { "tier": "free" }, categories: ["typography"]`
- paragraph: `meta: { "tier": "free" }, categories: ["typography"]`

**Premium tier (9 items) - composite/layout/animated:**
- box: `meta: { "tier": "premium" }, categories: ["layout"]`
- flex: `meta: { "tier": "premium" }, categories: ["layout"]`
- flex-row: `meta: { "tier": "premium" }, categories: ["layout"]`
- flex-col: `meta: { "tier": "premium" }, categories: ["layout"]`
- grid: `meta: { "tier": "premium" }, categories: ["layout"]`
- button: `meta: { "tier": "premium" }, categories: ["form"]`
- social-links: `meta: { "tier": "premium" }, categories: ["animation", "social"]`
- hero: `meta: { "tier": "premium" }, categories: ["composite", "marketing"]`
- appear: `meta: { "tier": "premium" }, categories: ["animation"]`

Add meta and categories fields after the existing fields in each item.
  </action>
  <verify>Run `cat registry.json | jq '.items[] | select(.type == "registry:ui") | {name, meta, categories}'` to confirm all 11 UI items have tier and categories</verify>
  <done>All UI items have appropriate tier (2 free, 9 premium) and category arrays</done>
</task>

<task type="auto">
  <name>Task 3: Validate registry JSON structure</name>
  <files>registry.json</files>
  <action>
Validate the updated registry.json:
1. Run `cat registry.json | jq '.'` to verify valid JSON
2. Count items by tier: `cat registry.json | jq '[.items[].meta.tier] | group_by(.) | map({tier: .[0], count: length})'`
3. List all categories used: `cat registry.json | jq '[.items[].categories[]] | unique'`
4. Verify no items are missing tier metadata: `cat registry.json | jq '.items[] | select(.meta.tier == null) | .name'`
  </action>
  <verify>JSON is valid, tier counts are correct (5 free, 9 premium), all items have tier metadata</verify>
  <done>Registry JSON valid, all items have tier metadata, categories are consistent</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cat registry.json | jq '.'` outputs valid JSON
- [ ] All 14 items have `meta.tier` field
- [ ] Free tier count: 5 (3 lib + 2 typography)
- [ ] Premium tier count: 9 (layout + form + animation + composite)
- [ ] All items have at least one category
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No JSON syntax errors
- Tier metadata correctly applied per classification rules
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-tiering/02-01-SUMMARY.md`
</output>
