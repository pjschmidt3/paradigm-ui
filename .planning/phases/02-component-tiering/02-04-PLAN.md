---
phase: 02-component-tiering
plan: 04
type: execute
---

<objective>
Register final batch of atomic components from src/components/ui as free tier items (R-T alphabetically).

Purpose: Complete atomic component registration with free tier metadata.
Output: Registry.json with all 53 atomic components registered, completing Phase 2.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-tiering/02-03-SUMMARY.md (if exists)
@registry.json
@src/components/ui/

**Components to register (batch 3 - R through T + remaining):**
1. radio-group
2. resizable
3. scroll-area
4. select
5. separator
6. sheet
7. sidebar
8. skeleton
9. slider
10. sonner
11. spinner
12. switch
13. table
14. tabs
15. textarea
16. toggle-group
17. toggle
18. tooltip
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register form and layout components (R-S)</name>
  <files>registry.json</files>
  <action>
Add registry items for:

1. radio-group - form category, depends on @radix-ui/react-radio-group
2. resizable - layout category, depends on react-resizable-panels
3. scroll-area - layout category, depends on @radix-ui/react-scroll-area
4. select - form category, depends on @radix-ui/react-select
5. separator - layout category, depends on @radix-ui/react-separator
6. sheet - overlay category, depends on @radix-ui/react-dialog
7. sidebar - navigation category, check dependencies
8. skeleton - feedback category, minimal deps
9. slider - form category, depends on @radix-ui/react-slider
10. sonner - feedback category, depends on sonner
11. spinner - feedback category, minimal deps
12. switch - form category, depends on @radix-ui/react-switch

For each: Read file, add with meta.tier="free", appropriate categories.
  </action>
  <verify>12 new items added</verify>
  <done>12 components registered as free tier</done>
</task>

<task type="auto">
  <name>Task 2: Register remaining components (T)</name>
  <files>registry.json</files>
  <action>
Add registry items for remaining components:

1. table - data-display category, minimal deps
2. tabs - navigation category, depends on @radix-ui/react-tabs
3. textarea - form category, minimal deps
4. toggle-group - form category, depends on @radix-ui/react-toggle-group
5. toggle - form category, depends on @radix-ui/react-toggle
6. tooltip - overlay category, depends on @radix-ui/react-tooltip

For each: Read file, add with meta.tier="free", appropriate categories.
  </action>
  <verify>Total items should be 66 (48 + 18)</verify>
  <done>6 more components registered, all atomic components complete</done>
</task>

<task type="auto">
  <name>Task 3: Final validation and tier summary</name>
  <files>registry.json</files>
  <action>
Complete validation:
1. Verify JSON valid
2. Generate tier summary: `cat registry.json | jq '[.items[].meta.tier] | group_by(.) | map({tier: .[0], count: length})'`
3. Verify all 53 atomic components from src/components/ui are registered
4. List any src/components/ui files NOT in registry
5. Generate category summary: `cat registry.json | jq '[.items[].categories[]] | group_by(.) | map({category: .[0], count: length})'`

Expected tier breakdown:
- Free: ~56 (3 lib + 2 typography + ~51 atomic)
- Premium: 9 (layout/composite components from registry/new-york/ui)
  </action>
  <verify>All 53 atomic components registered, tier counts correct</verify>
  <done>Phase 2 complete - all components have tier metadata, ready for Phase 3</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Valid JSON
- [ ] Total items: ~67 (14 original + 53 atomic)
- [ ] Free tier: ~58 items
- [ ] Premium tier: 9 items
- [ ] All src/components/ui/*.tsx files have registry entries
- [ ] No duplicate names
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- All atomic components registered as free tier
- Phase 2 complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-tiering/02-04-SUMMARY.md`

Include in summary:
- Total component count by tier
- Category distribution
- Any components excluded and why
- Readiness for Phase 3 (Documentation & Polish)
</output>
