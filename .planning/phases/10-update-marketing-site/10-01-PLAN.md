---
phase: 10-update-marketing-site
type: execute
---

<objective>
Update marketing site to showcase v0.2.0 additions: hooks, enhanced components, aesthetic cards, and premium blocks.

Purpose: Market the v0.2.0 library expansion with live demos and updated feature lists
Output: Updated marketing site with HooksSection, expanded ComponentsSection, and accurate tier feature lists
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site/src/components/paradigm.ts
@site/src/components/components-section.tsx
@site/src/components/pricing-section.tsx
@site/src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update barrel exports and create HooksSection</name>
  <files>site/src/components/paradigm.ts, site/src/components/hooks-section.tsx</files>
  <action>
    1. Update paradigm.ts with new exports for v0.2.0 components:
       - ProfileCard, StatsCard, TestimonialCard, FeatureCard from @paradigm/[name]
       - Blockquote, List, Callout from @paradigm/[name]
       - Badge (already exists? verify) from @paradigm/badge
       - Avatar, AvatarStatus from @paradigm/avatar

    2. Create hooks-section.tsx showcasing the 7 new hooks:
       - Section header "Hooks" with description about React hooks
       - 3-4 interactive demos using ComponentPreview pattern:
         a) useDisclosure: Toggle a panel open/closed
         b) useToggle: Boolean toggle demo (dark mode style)
         c) useMediaQuery: Show current breakpoint
         d) useClickOutside: Dropdown that closes on outside click
       - All hooks marked as "free" tier
       - Import hooks from @paradigm/* (add hook exports to paradigm.ts)

    Note: Hooks cannot be demonstrated via static JSX code blocks - create interactive client components that USE the hooks.
  </action>
  <verify>TypeScript compilation passes in site/, hooks-section.tsx renders without errors</verify>
  <done>HooksSection component exists with 3-4 hook demos, paradigm.ts exports updated</done>
</task>

<task type="auto">
  <name>Task 2: Update ComponentsSection with new showcases</name>
  <files>site/src/components/components-section.tsx</files>
  <action>
    Add ComponentPreview entries for v0.2.0 additions after existing previews:

    1. ProfileCard (free) - Show avatar, name, title, bio layout
    2. StatsCard (free) - Show value, label, trend indicator
    3. Callout (free) - Show info/warning/success variants
    4. FeatureCard (free) - Show icon + title + description pattern

    For each preview:
    - Import from "./paradigm"
    - Use ComponentPreview with tier="free"
    - Provide realistic code example in code prop
    - Render live preview as children

    Keep existing previews (FlexRow, Grid, Button, Hero, Typography, CodeBlock).
  </action>
  <verify>npm run dev in site/ shows new component previews rendering correctly</verify>
  <done>ComponentsSection shows 4 new component previews with code examples</done>
</task>

<task type="auto">
  <name>Task 3: Update PricingSection and integrate HooksSection</name>
  <files>site/src/components/pricing-section.tsx, site/src/app/page.tsx</files>
  <action>
    1. Update pricing-section.tsx feature arrays:

    freeTierFeatures should include:
    - '7 React hooks (useDisclosure, useToggle, useMediaQuery, etc.)'
    - '10+ aesthetic cards (ProfileCard, StatsCard, etc.)'
    - 'Content components (Blockquote, List, Callout)'
    - Keep existing: Layout, Typography, 50+ atomic, TypeScript, TailwindCSS

    premiumFeatures should include:
    - 'PricingTable and CTASection blocks'
    - 'Portfolio blocks (ProjectCard, Timeline)'
    - 'Page layout blocks (PageHeader, SectionWrapper)'
    - Keep existing: Hero, SocialLinks, CodeBlock, Appear, Button variants, support

    2. Update page.tsx to import and render HooksSection between HeroSection and ComponentsSection:

    ```
    <HeroSection />
    <HooksSection />
    <ComponentsSection />
    ```
  </action>
  <verify>npm run dev in site/ shows updated pricing features and HooksSection in correct position</verify>
  <done>Marketing site shows hooks, updated component count, and accurate tier features</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cd site && npm run build` succeeds without errors
- [ ] HooksSection renders with interactive hook demos
- [ ] ComponentsSection shows 4 new component previews
- [ ] PricingSection reflects v0.2.0 additions accurately
- [ ] All tier badges (free/premium) are correctly assigned
</verification>

<success_criteria>

- All 3 tasks completed
- Site builds without TypeScript errors
- New hooks showcased with interactive demos
- New aesthetic components previewed with code examples
- Pricing tiers accurately reflect v0.2.0 additions
- v0.2.0 milestone complete (Phase 10 ships)
</success_criteria>

<output>
After completion, create `.planning/phases/10-update-marketing-site/10-01-SUMMARY.md`:

# Phase 10 Plan 1: Update Marketing Site Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Phase Readiness
</output>
