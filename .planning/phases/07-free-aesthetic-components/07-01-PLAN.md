---
phase: 07-free-aesthetic-components
plan: 01
type: execute
---

<objective>
Create card variant components that compose the existing Card primitives into ready-to-use aesthetic components.

Purpose: Provide developers with pre-styled, production-ready card patterns that go beyond the basic Card primitive.
Output: ProfileCard, StatsCard, TestimonialCard, FeatureCard components in registry/new-york/ui/ with free tier.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/components/ui/card.tsx
@registry/new-york/ui/hero/hero.tsx
@registry/new-york/ui/heading/heading.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileCard and StatsCard components</name>
  <files>registry/new-york/ui/profile-card/profile-card.tsx, registry/new-york/ui/stats-card/stats-card.tsx</files>
  <action>
    Create two card variant components following existing patterns:

    **ProfileCard** (`registry/new-york/ui/profile-card/profile-card.tsx`):
    - Props: avatar (string URL), name, title/role, bio (optional), actions (ReactNode for buttons)
    - Compose Card, CardHeader, CardContent, CardFooter from `@/src/components/ui/card`
    - Use Avatar from `@/src/components/ui/avatar` for the avatar image
    - Center-aligned layout with avatar at top, name/title below, bio in content, actions in footer
    - Export ProfileCard and ProfileCardProps

    **StatsCard** (`registry/new-york/ui/stats-card/stats-card.tsx`):
    - Props: value (string/number), label, icon (optional LucideIcon), trend (optional: up/down/neutral with percentage)
    - Compose Card, CardContent from `@/src/components/ui/card`
    - Large value display, smaller label below, icon top-right corner
    - Trend indicator with ArrowUp/ArrowDown icons and color coding (green up, red down)
    - Export StatsCard and StatsCardProps

    Use cn() for all className composition. Follow kebab-case file naming. Use single quotes, no semicolons.
  </action>
  <verify>
    - Files exist at registry/new-york/ui/profile-card/profile-card.tsx and registry/new-york/ui/stats-card/stats-card.tsx
    - TypeScript compiles: `npx tsc --noEmit`
    - Components export correctly
  </verify>
  <done>ProfileCard and StatsCard components created with proper typing and Card primitive composition</done>
</task>

<task type="auto">
  <name>Task 2: Create TestimonialCard and FeatureCard, register all in registry.json</name>
  <files>registry/new-york/ui/testimonial-card/testimonial-card.tsx, registry/new-york/ui/feature-card/feature-card.tsx, registry/new-york/ui/index.tsx, registry.json</files>
  <action>
    Create remaining card variants and register all:

    **TestimonialCard** (`registry/new-york/ui/testimonial-card/testimonial-card.tsx`):
    - Props: quote (string), author, role (optional), avatar (optional string URL), rating (optional 1-5)
    - Compose Card, CardContent, CardFooter from `@/src/components/ui/card`
    - Quote with quotation marks styling, star rating if provided, author info in footer
    - Use Avatar for author image when provided
    - Export TestimonialCard and TestimonialCardProps

    **FeatureCard** (`registry/new-york/ui/feature-card/feature-card.tsx`):
    - Props: icon (LucideIcon), title, description, href (optional for link behavior)
    - Compose Card, CardHeader, CardContent from `@/src/components/ui/card`
    - Icon prominently displayed (in colored circle background), title as heading, description as body
    - When href provided, entire card is clickable with hover effect
    - Export FeatureCard and FeatureCardProps

    **Update index.tsx** - Add exports for all 4 new card components

    **Update registry.json** - Add entries for all 4 cards:
    - type: "registry:ui"
    - meta: { "tier": "free" }
    - categories: ["cards"]
    - registryDependencies: ["utils", "card", "avatar"] (as appropriate per component)
    - dependencies: ["lucide-react"] for ones using icons
  </action>
  <verify>
    - Files exist at registry/new-york/ui/testimonial-card/testimonial-card.tsx and registry/new-york/ui/feature-card/feature-card.tsx
    - registry/new-york/ui/index.tsx exports all 4 new components
    - registry.json has 4 new entries with tier: "free"
    - TypeScript compiles: `npx tsc --noEmit`
    - JSON valid: `node -e "require('./registry.json')"`
  </verify>
  <done>TestimonialCard and FeatureCard created, all 4 card variants exported and registered as free tier</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 card component files exist in registry/new-york/ui/
- [ ] All components exported from registry/new-york/ui/index.tsx
- [ ] registry.json has 4 new free-tier card entries
- [ ] `npx tsc --noEmit` passes
- [ ] `node -e "require('./registry.json')"` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 4 card variant components (ProfileCard, StatsCard, TestimonialCard, FeatureCard) created
- Components properly compose existing Card primitives
- All registered as free tier in registry.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-free-aesthetic-components/07-01-SUMMARY.md`
</output>
