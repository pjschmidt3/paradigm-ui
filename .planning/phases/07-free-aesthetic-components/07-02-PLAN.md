---
phase: 07-free-aesthetic-components
plan: 02
type: execute
---

<objective>
Create content display components for rich text presentation beyond basic typography.

Purpose: Provide developers with styled content components for quotes, lists, and highlighted information.
Output: Blockquote, List (with variants), and Callout components in registry/new-york/ui/ with free tier.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@registry/new-york/ui/paragraph/paragraph.tsx
@registry/new-york/ui/heading/heading.tsx
@src/components/ui/alert.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Blockquote and List components</name>
  <files>registry/new-york/ui/blockquote/blockquote.tsx, registry/new-york/ui/list/list.tsx</files>
  <action>
    Create content display components following existing patterns:

    **Blockquote** (`registry/new-york/ui/blockquote/blockquote.tsx`):
    - Props: children, cite (optional attribution), variant ('default' | 'bordered' | 'filled')
    - Use CVA for variants:
      - default: italic text, left border accent
      - bordered: full border with padding, subtle background
      - filled: solid muted background with contrasting text
    - Optional cite renders as footer with em-dash prefix
    - Semantic blockquote element with proper accessibility
    - Export Blockquote, BlockquoteProps, blockquoteVariants

    **List** (`registry/new-york/ui/list/list.tsx`):
    - Props: items (string[] or ReactNode[]), variant ('bullet' | 'numbered' | 'check' | 'none'), spacing ('tight' | 'normal' | 'relaxed')
    - Use CVA for list styling variants
    - bullet: standard ul with disc markers
    - numbered: ol with decimal markers
    - check: ul with check icons (lucide-react Check icon) in green
    - none: no markers, clean list
    - spacing controls gap between items
    - Export List, ListProps, ListItem (for manual composition), listVariants
  </action>
  <verify>
    - Files exist at registry/new-york/ui/blockquote/blockquote.tsx and registry/new-york/ui/list/list.tsx
    - TypeScript compiles: `npx tsc --noEmit`
    - Components use CVA for variants
  </verify>
  <done>Blockquote and List components created with CVA variants and proper semantic HTML</done>
</task>

<task type="auto">
  <name>Task 2: Create Callout component, register all in registry.json</name>
  <files>registry/new-york/ui/callout/callout.tsx, registry/new-york/ui/index.tsx, registry.json</files>
  <action>
    Create Callout and register all content components:

    **Callout** (`registry/new-york/ui/callout/callout.tsx`):
    - Props: children, title (optional), variant ('info' | 'warning' | 'success' | 'error' | 'note'), icon (optional override LucideIcon)
    - Use CVA for variants with appropriate colors:
      - info: blue tones, Info icon default
      - warning: amber/yellow tones, AlertTriangle icon default
      - success: green tones, CheckCircle icon default
      - error: red tones, XCircle icon default
      - note: gray/muted tones, Lightbulb icon default
    - Structure: icon on left, title (if provided) as bold heading, children as body
    - Rounded border with subtle background matching variant
    - Export Callout, CalloutProps, calloutVariants

    **Update index.tsx** - Add exports for Blockquote, List, ListItem, Callout

    **Update registry.json** - Add entries for all 3 components:
    - type: "registry:ui"
    - meta: { "tier": "free" }
    - categories: ["typography"] for Blockquote, ["typography", "data-display"] for List, ["feedback"] for Callout
    - registryDependencies: ["utils"]
    - dependencies: ["class-variance-authority"] for all, ["lucide-react"] for List and Callout
  </action>
  <verify>
    - File exists at registry/new-york/ui/callout/callout.tsx
    - registry/new-york/ui/index.tsx exports Blockquote, List, ListItem, Callout
    - registry.json has 3 new entries with tier: "free"
    - TypeScript compiles: `npx tsc --noEmit`
    - JSON valid: `node -e "require('./registry.json')"`
  </verify>
  <done>Callout created, all 3 content display components exported and registered as free tier</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 content component files exist in registry/new-york/ui/
- [ ] All components exported from registry/new-york/ui/index.tsx
- [ ] registry.json has 3 new free-tier content entries
- [ ] `npx tsc --noEmit` passes
- [ ] `node -e "require('./registry.json')"` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 3 content display components (Blockquote, List, Callout) created with CVA variants
- All registered as free tier in registry.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-free-aesthetic-components/07-02-SUMMARY.md`
</output>
