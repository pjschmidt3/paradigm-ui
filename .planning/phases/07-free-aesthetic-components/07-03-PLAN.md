---
phase: 07-free-aesthetic-components
plan: 03
type: execute
---

<objective>
Create navigation helper components for enhanced link and step-based UI patterns.

Purpose: Provide developers with styled navigation components beyond basic anchor tags.
Output: LinkButton, NavLink, and StepIndicator components in registry/new-york/ui/ with free tier.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/components/ui/button.tsx
@registry/new-york/ui/button/button.tsx
@registry/new-york/ui/social-links/social-links.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LinkButton and NavLink components</name>
  <files>registry/new-york/ui/link-button/link-button.tsx, registry/new-york/ui/nav-link/nav-link.tsx</files>
  <action>
    Create navigation components following existing patterns:

    **LinkButton** (`registry/new-york/ui/link-button/link-button.tsx`):
    - Props: href, children, icon (optional LucideIcon), iconPosition ('left' | 'right'), external (boolean), variant (inherit from button variants), size (inherit from button sizes)
    - Renders as anchor tag styled like Button using buttonVariants from registry Button
    - When external=true: adds target="_blank" rel="noopener noreferrer" and optional ExternalLink icon
    - Icon renders before or after children based on iconPosition
    - Supports all button variants (default, destructive, outline, secondary, ghost, link)
    - Use forwardRef for proper ref forwarding to anchor element
    - Export LinkButton, LinkButtonProps

    **NavLink** (`registry/new-york/ui/nav-link/nav-link.tsx`):
    - Props: href, children, active (boolean), icon (optional LucideIcon), badge (optional string/number for notification count)
    - Use CVA for active/inactive states:
      - inactive: muted text, no background
      - active: primary text, subtle background highlight, font-medium
    - Icon renders before children with proper spacing
    - Badge renders as small pill after children (uses Badge component if badgeVariants exist, otherwise inline styles)
    - Hover state: slight background change on inactive links
    - Use forwardRef for proper ref forwarding
    - Export NavLink, NavLinkProps, navLinkVariants
  </action>
  <verify>
    - Files exist at registry/new-york/ui/link-button/link-button.tsx and registry/new-york/ui/nav-link/nav-link.tsx
    - TypeScript compiles: `npx tsc --noEmit`
    - LinkButton inherits button styling properly
    - NavLink has active/inactive CVA variants
  </verify>
  <done>LinkButton and NavLink components created with proper anchor semantics and styling</done>
</task>

<task type="auto">
  <name>Task 2: Create StepIndicator component, register all, update ROADMAP</name>
  <files>registry/new-york/ui/step-indicator/step-indicator.tsx, registry/new-york/ui/index.tsx, registry.json, .planning/ROADMAP.md</files>
  <action>
    Create StepIndicator and finalize Phase 7:

    **StepIndicator** (`registry/new-york/ui/step-indicator/step-indicator.tsx`):
    - Props: steps (array of { label: string, description?: string }), currentStep (number, 0-indexed), orientation ('horizontal' | 'vertical'), variant ('dots' | 'numbers' | 'icons')
    - Use CVA for step states: completed (checkmark, primary color), current (highlighted, ring), upcoming (muted)
    - Connectors between steps: solid line for completed, dashed for upcoming
    - variant options:
      - dots: simple circles
      - numbers: numbered circles (1, 2, 3...)
      - icons: Check icon for completed, current step number, empty circle for upcoming
    - horizontal: flex-row with flex-grow connectors
    - vertical: flex-col with fixed-height connectors
    - Export StepIndicator, StepIndicatorProps, Step type, stepIndicatorVariants

    **Update index.tsx** - Add exports for LinkButton, NavLink, StepIndicator

    **Update registry.json** - Add entries for all 3 navigation components:
    - type: "registry:ui"
    - meta: { "tier": "free" }
    - categories: ["navigation"] for all
    - registryDependencies: ["utils", "button"] for LinkButton, ["utils"] for others
    - dependencies: ["class-variance-authority", "lucide-react"] for all

    **Update ROADMAP.md** - Mark 07-01, 07-02, 07-03 as completed with descriptions
  </action>
  <verify>
    - File exists at registry/new-york/ui/step-indicator/step-indicator.tsx
    - registry/new-york/ui/index.tsx exports LinkButton, NavLink, StepIndicator
    - registry.json has 3 new entries with tier: "free"
    - ROADMAP.md shows 07-01, 07-02, 07-03 as completed
    - TypeScript compiles: `npx tsc --noEmit`
    - JSON valid: `node -e "require('./registry.json')"`
  </verify>
  <done>StepIndicator created, all Phase 7 components exported, registered, and ROADMAP updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 navigation component files exist in registry/new-york/ui/
- [ ] All components exported from registry/new-york/ui/index.tsx
- [ ] registry.json has 3 new free-tier navigation entries
- [ ] ROADMAP.md updated with Phase 7 completion
- [ ] `npx tsc --noEmit` passes
- [ ] `node -e "require('./registry.json')"` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 3 navigation helper components (LinkButton, NavLink, StepIndicator) created
- All registered as free tier in registry.json
- Phase 7 complete - 10 new aesthetic components total (4 cards, 3 content, 3 navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/07-free-aesthetic-components/07-03-SUMMARY.md`

Phase 7 complete summary:
- 07-01: 4 card variants (ProfileCard, StatsCard, TestimonialCard, FeatureCard)
- 07-02: 3 content display (Blockquote, List, Callout)
- 07-03: 3 navigation helpers (LinkButton, NavLink, StepIndicator)
</output>
