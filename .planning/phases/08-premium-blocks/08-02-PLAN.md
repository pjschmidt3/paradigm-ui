---
phase: 08-premium-blocks
plan: 02
type: execute
---

<objective>
Create portfolio premium blocks: ProjectCard and Timeline.

Purpose: Add portfolio/showcase components for agency and developer portfolio sites - common premium needs.
Output: 2 premium components with tests and Storybook stories, registered in registry.json.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/08-premium-blocks/08-01-SUMMARY.md
@registry/new-york/ui/link-button/link-button.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectCard component</name>
  <files>
    registry/new-york/ui/project-card/project-card.tsx,
    registry/new-york/ui/project-card/project-card.test.tsx,
    registry/new-york/ui/project-card/project-card.stories.tsx,
    registry/new-york/ui/project-card/index.ts
  </files>
  <action>
    Create ProjectCard for portfolio showcases:

    **Props:**
    - title: string
    - description?: string
    - image?: { src: string; alt: string } (project screenshot/preview)
    - tags?: string[] (tech stack badges)
    - links?: Array<{ label: string; href: string; icon?: ReactNode }>
    - featured?: boolean (larger card, highlighted)
    - variant?: 'default' | 'horizontal' | 'overlay' (image layout)
    - className?: string

    Implementation:
    - default: vertical card with image on top
    - horizontal: image left, content right (for featured projects)
    - overlay: image as background with text overlay (dark gradient)
    - Image uses aspect-video container with object-cover
    - Tags rendered as Badge components (outline variant)
    - Links rendered as LinkButton components with icons
    - Featured gets larger image height and shadow-lg
    - CVA for variant styling

    Tests: render title/description, image, tags as badges, links, variant classes.
    Stories: Default, Horizontal, Overlay, Featured, With tech stack tags.
  </action>
  <verify>npm test -- project-card.test.tsx passes</verify>
  <done>ProjectCard renders with image, tags, links, and layout variants</done>
</task>

<task type="auto">
  <name>Task 2: Create Timeline component</name>
  <files>
    registry/new-york/ui/timeline/timeline.tsx,
    registry/new-york/ui/timeline/timeline.test.tsx,
    registry/new-york/ui/timeline/timeline.stories.tsx,
    registry/new-york/ui/timeline/index.ts
  </files>
  <action>
    Create Timeline and TimelineItem for chronological displays:

    **TimelineItem props:**
    - title: string
    - description?: ReactNode | string
    - date?: string (formatted date display)
    - icon?: ReactNode (custom icon for the dot)
    - status?: 'completed' | 'current' | 'upcoming' (dot styling)
    - className?: string

    **Timeline props:**
    - items?: TimelineItemProps[] (alternative to children)
    - children?: ReactNode (TimelineItem children)
    - orientation?: 'vertical' | 'horizontal'
    - linePosition?: 'left' | 'center' | 'right' (for vertical)
    - className?: string

    Implementation:
    - Vertical: items stacked with connecting line
    - Horizontal: items in row with connecting line (scrollable on mobile)
    - Dot: 12px circle with border, filled for completed, ring for current, muted for upcoming
    - Line: 2px border-muted connecting dots
    - Date positioned above title (muted text)
    - linePosition='center' for alternating left/right content (resume style)
    - CVA for orientation and status styling

    Tests: render items, status dot styling, orientation classes, alternating layout.
    Stories: Vertical timeline, Horizontal timeline, With icons, Alternating (center line).
  </action>
  <verify>npm test -- timeline.test.tsx passes</verify>
  <done>Timeline renders with items, status indicators, and orientation options</done>
</task>

<task type="auto">
  <name>Task 3: Register premium portfolio blocks</name>
  <files>
    registry/new-york/ui/index.tsx,
    registry.json,
    src/index.ts
  </files>
  <action>
    Register both components:

    1. Add exports to registry/new-york/ui/index.tsx:
       - export * from './project-card'
       - export * from './timeline'

    2. Add to registry.json with tier: "premium", categories: ["portfolio"]:
       - project-card entry
       - timeline entry

    3. Add to src/index.ts package exports
  </action>
  <verify>grep -q "project-card" registry.json && grep -q "timeline" registry.json</verify>
  <done>Both components registered as premium tier in registry</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests including new ones
- [ ] `npm run storybook` builds without errors
- [ ] ProjectCard stories render correctly
- [ ] Timeline stories render correctly
- [ ] registry.json has both entries with tier: "premium"
</verification>

<success_criteria>
- ProjectCard with image layouts, tech tags, action links
- Timeline with vertical/horizontal orientations and status indicators
- Unit tests for both components
- Storybook stories demonstrating all variants
- Both registered as premium tier
</success_criteria>

<output>
After completion, create `.planning/phases/08-premium-blocks/08-02-SUMMARY.md`
</output>
