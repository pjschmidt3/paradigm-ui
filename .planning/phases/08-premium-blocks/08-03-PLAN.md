---
phase: 08-premium-blocks
plan: 03
type: execute
---

<objective>
Create page layout premium blocks: PageHeader and SectionWrapper.

Purpose: Add structural page components that provide consistent layout patterns - saves developers from reinventing common page structures.
Output: 2 premium components with tests and Storybook stories, registered in registry.json.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/08-premium-blocks/08-01-SUMMARY.md
@.planning/phases/08-premium-blocks/08-02-SUMMARY.md
@registry/new-york/ui/heading/heading.tsx
@registry/new-york/ui/paragraph/paragraph.tsx
@src/components/ui/breadcrumb.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PageHeader component</name>
  <files>
    registry/new-york/ui/page-header/page-header.tsx,
    registry/new-york/ui/page-header/page-header.test.tsx,
    registry/new-york/ui/page-header/page-header.stories.tsx,
    registry/new-york/ui/page-header/index.ts
  </files>
  <action>
    Create PageHeader for consistent page title areas:

    **Props:**
    - title: string
    - description?: ReactNode | string
    - breadcrumbs?: Array<{ label: string; href?: string }> (breadcrumb trail)
    - actions?: ReactNode (buttons/actions slot)
    - badge?: { label: string; variant?: BadgeVariant } (status badge next to title)
    - backLink?: { label: string; href: string } (back navigation)
    - variant?: 'default' | 'centered' | 'compact'
    - divider?: boolean (show bottom border)
    - className?: string

    Implementation:
    - Breadcrumbs at top using Breadcrumb component
    - Back link with arrow icon above title
    - Title as Heading level 1 with badge inline
    - Description as Paragraph below title
    - Actions slot positioned: right on default/compact, below on centered
    - default: left-aligned, full spacing
    - centered: centered text, stacked layout
    - compact: reduced padding, smaller title
    - divider adds border-b border-border mb-6

    Tests: render title/description, breadcrumbs, actions slot, badge, variant classes.
    Stories: Default, With breadcrumbs, With actions, Centered, Compact.
  </action>
  <verify>npm test -- page-header.test.tsx passes</verify>
  <done>PageHeader renders with title, breadcrumbs, actions, and layout variants</done>
</task>

<task type="auto">
  <name>Task 2: Create SectionWrapper component</name>
  <files>
    registry/new-york/ui/section-wrapper/section-wrapper.tsx,
    registry/new-york/ui/section-wrapper/section-wrapper.test.tsx,
    registry/new-york/ui/section-wrapper/section-wrapper.stories.tsx,
    registry/new-york/ui/section-wrapper/index.ts
  </files>
  <action>
    Create SectionWrapper for consistent page sections:

    **Props:**
    - title?: string
    - description?: ReactNode | string
    - children: ReactNode (section content)
    - actions?: ReactNode (section-level actions)
    - id?: string (for anchor links)
    - variant?: 'default' | 'card' | 'inset'
    - background?: 'default' | 'muted' | 'accent'
    - divider?: 'top' | 'bottom' | 'both' | 'none'
    - spacing?: 'sm' | 'md' | 'lg' | 'xl'
    - className?: string

    Implementation:
    - default: plain section with title/description header
    - card: wrapped in Card with padding
    - inset: negative margin to break out of container (for full-width sections)
    - Background colors via CVA
    - Dividers as border-t/border-b border-border
    - Spacing controls py-* values (sm=8, md=12, lg=16, xl=24)
    - Title as Heading level 2, description as Paragraph
    - Actions float right of title
    - id prop for section anchoring

    Tests: render title/description/children, variant classes, background, dividers, spacing.
    Stories: Default, Card variant, With actions, Muted background, Full spacing options.
  </action>
  <verify>npm test -- section-wrapper.test.tsx passes</verify>
  <done>SectionWrapper renders with header, content slot, and styling options</done>
</task>

<task type="auto">
  <name>Task 3: Register premium layout blocks and update state</name>
  <files>
    registry/new-york/ui/index.tsx,
    registry.json,
    src/index.ts,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    Register components and update project state:

    1. Add exports to registry/new-york/ui/index.tsx:
       - export * from './page-header'
       - export * from './section-wrapper'

    2. Add to registry.json with tier: "premium", categories: ["layout"]:
       - page-header entry
       - section-wrapper entry

    3. Add to src/index.ts package exports

    4. Update .planning/STATE.md:
       - Phase: 8 of 9
       - Plan: 3 of 3 in current phase
       - Status: Phase complete

    5. Update .planning/ROADMAP.md:
       - Mark 08-01, 08-02, 08-03 as complete
       - Update Plans count to 3/3
  </action>
  <verify>grep -q "page-header" registry.json && grep -q "section-wrapper" registry.json</verify>
  <done>Components registered, STATE.md and ROADMAP.md updated for Phase 8 complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests including new ones
- [ ] `npm run storybook` builds without errors
- [ ] PageHeader stories render correctly
- [ ] SectionWrapper stories render correctly
- [ ] registry.json has both entries with tier: "premium"
- [ ] STATE.md shows Phase 8 complete
- [ ] ROADMAP.md Phase 8 shows 3/3 plans complete
</verification>

<success_criteria>
- PageHeader with breadcrumbs, title, description, actions, variants
- SectionWrapper with header, content, background, dividers, spacing
- Unit tests for both components
- Storybook stories demonstrating all variants
- Both registered as premium tier
- Phase 8 marked complete in project state
</success_criteria>

<output>
After completion, create `.planning/phases/08-premium-blocks/08-03-SUMMARY.md`
</output>
