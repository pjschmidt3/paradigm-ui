---
phase: 08-premium-blocks
plan: 01
type: execute
---

<objective>
Create marketing premium blocks: PricingTable and CTASection.

Purpose: Add high-value composed marketing components that justify premium tier - these save developers significant time building landing pages.
Output: 2 premium components with tests and Storybook stories, registered in registry.json.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@registry/new-york/ui/button/button.tsx
@registry/new-york/ui/heading/heading.tsx
@registry/new-york/ui/paragraph/paragraph.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PricingTable component</name>
  <files>
    registry/new-york/ui/pricing-table/pricing-table.tsx,
    registry/new-york/ui/pricing-table/pricing-table.test.tsx,
    registry/new-york/ui/pricing-table/pricing-table.stories.tsx,
    registry/new-york/ui/pricing-table/index.ts
  </files>
  <action>
    Create PricingTable and PricingTier sub-components:

    **PricingTier props:**
    - name: string (tier name like "Basic", "Pro")
    - price: string | number (display price)
    - period?: string ("month", "year", custom)
    - description?: string
    - features: string[] (list of included features)
    - cta: { label: string; href?: string; onClick?: () => void }
    - popular?: boolean (adds "Popular" badge, highlighted border)
    - className?: string

    **PricingTable props:**
    - tiers: PricingTierProps[] (array of tier configs)
    - className?: string

    Implementation:
    - Use Card component for each tier
    - Badge for "Popular" indicator (position top-right or as ribbon)
    - List component for features (with check icons)
    - Button for CTA
    - Popular tier gets ring-2 ring-primary border highlight
    - Responsive: stack on mobile, row on desktop (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)

    Tests: render tiers, popular badge display, CTA rendering, custom className.
    Stories: Default (3 tiers), Popular highlighted, Two-tier comparison.
  </action>
  <verify>npm test -- pricing-table.test.tsx passes, npm run storybook shows stories</verify>
  <done>PricingTable renders tiers with features, CTAs, and popular highlighting</done>
</task>

<task type="auto">
  <name>Task 2: Create CTASection component</name>
  <files>
    registry/new-york/ui/cta-section/cta-section.tsx,
    registry/new-york/ui/cta-section/cta-section.test.tsx,
    registry/new-york/ui/cta-section/cta-section.stories.tsx,
    registry/new-york/ui/cta-section/index.ts
  </files>
  <action>
    Create CTASection component for call-to-action blocks:

    **Props:**
    - heading: ReactNode | string
    - description?: ReactNode | string
    - primaryAction: { label: string; href?: string; onClick?: () => void }
    - secondaryAction?: { label: string; href?: string; onClick?: () => void }
    - variant?: 'default' | 'centered' | 'split' (layout variants)
    - background?: 'default' | 'muted' | 'primary' | 'gradient'
    - className?: string

    Implementation:
    - default: left-aligned text, buttons on right (flex justify-between)
    - centered: centered text, stacked buttons below
    - split: two-column with text left, large CTA right
    - Use Heading level 2 for heading
    - Use Paragraph for description
    - Button components for actions (primary = default, secondary = outline)
    - CVA for variant and background styling
    - Padding: py-16 px-6 standard

    Tests: render heading/description, both actions, variant classes, background classes.
    Stories: Default, Centered, Split, With gradient background.
  </action>
  <verify>npm test -- cta-section.test.tsx passes, npm run storybook shows stories</verify>
  <done>CTASection renders with layout variants and background options</done>
</task>

<task type="auto">
  <name>Task 3: Register premium marketing blocks</name>
  <files>
    registry/new-york/ui/index.tsx,
    registry.json,
    src/index.ts
  </files>
  <action>
    Register both components:

    1. Add exports to registry/new-york/ui/index.tsx:
       - export * from './pricing-table'
       - export * from './cta-section'

    2. Add to registry.json with tier: "premium", categories: ["marketing"]:
       - pricing-table entry
       - cta-section entry

    3. Add to src/index.ts package exports

    Follow existing patterns from hero and other premium components.
  </action>
  <verify>grep -q "pricing-table" registry.json && grep -q "cta-section" registry.json</verify>
  <done>Both components registered as premium tier in registry</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests including new ones
- [ ] `npm run storybook` builds without errors
- [ ] PricingTable stories render correctly
- [ ] CTASection stories render correctly
- [ ] registry.json has both entries with tier: "premium"
</verification>

<success_criteria>
- PricingTable component with tier cards, features list, popular badge, CTAs
- CTASection component with 3 layout variants and 4 background options
- Unit tests for both components
- Storybook stories demonstrating all variants
- Both registered as premium tier
</success_criteria>

<output>
After completion, create `.planning/phases/08-premium-blocks/08-01-SUMMARY.md`
</output>
