---
phase: 03-documentation-polish
plan: 02
type: execute
---

<objective>
Prepare registry for production distribution with validation, documentation, and build verification.

Purpose: Ensure the registry is ready for public consumption via shadcn CLI with proper documentation and validated metadata.
Output: Updated README with installation instructions, registry validation passing, verified build output.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-documentation-polish/03-01-SUMMARY.md (after plan 01)

Registry file:
@registry.json

Current README:
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create registry validation script</name>
  <files>scripts/validate-registry.js</files>
  <action>
Create a Node.js script that validates registry.json:
- All file paths in "files" array exist on disk
- All registryDependencies reference valid item names
- All items have required fields (name, type, description, files)
- No duplicate item names
- Tier metadata exists on all items
- Output validation results with pass/fail and details
Make it runnable via `node scripts/validate-registry.js`
  </action>
  <verify>node scripts/validate-registry.js exits 0 with "Registry valid" message</verify>
  <done>Validation script exists and passes for current registry.json</done>
</task>

<task type="auto">
  <name>Task 2: Update main README with installation instructions</name>
  <files>README.md</files>
  <action>
Update the project README with:
- Project description and value proposition
- Installation instructions via shadcn CLI: `npx shadcn add https://paradigm-ui.dev/registry.json/<component-name>`
- List of available components organized by tier (free vs premium)
- Quick example showing component usage
- Links to Storybook for interactive docs
- Development setup instructions
Keep it concise - link to detailed docs rather than duplicating content.
  </action>
  <verify>README.md contains installation instructions with shadcn CLI command</verify>
  <done>README.md updated with clear installation and usage instructions</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and export configuration</name>
  <files>package.json (verify only)</files>
  <action>
Verify the build produces correct artifacts:
- Run `npm run build` to verify build succeeds
- Check dist/ output includes ESM and CJS bundles
- Verify type definitions are generated
- Confirm package.json exports field is configured correctly
- Run registry validation to confirm all paths still valid
If any issues found, document them but do not fix (out of scope for this plan).
  </action>
  <verify>npm run build succeeds, dist/ contains expected output</verify>
  <done>Build verified working, any issues documented in summary</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/validate-registry.js` passes
- [ ] README.md contains installation instructions
- [ ] `npm run build` succeeds without errors
- [ ] Phase 3 complete
</verification>

<success_criteria>

- All tasks completed
- Registry validated with passing script
- README provides clear installation path
- Build verified working
- Phase 3: Documentation & Polish complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-polish/03-02-SUMMARY.md`
</output>
